Git status                                  Ví dụ-- check trạng thái các file thay đổi hoặc thêm mới <br>
git add .                                Ví dụ--- add các thay đổi(sửa/thêm mới/xóa)<br>
git commit -m"thay doi index"<br>
git push origin + "tên nhánh"               Ví dụ--- git push origin main<br>
git pull origin + "tên nhánh"               Ví dụ --- git pull origin main<br>
git checkout -b + "tên nhánh"                Ví dụ------ git checkout -b feature/quanhv<br>
git branch                                  Ví dụ -- Kiểm tra nhánh hiện tại<br>
