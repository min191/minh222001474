Git status                                  Ví dụ-- check trạng thái các file thay đổi hoặc thêm mới
git add .                                Ví dụ--- add các thay đổi(sửa/thêm mới/xóa)
git commit -m"thay doi index"
git push origin + "tên nhánh"               Ví dụ--- git push origin main
git pull origin + "tên nhánh"               Ví dụ --- git pull origin main
git checkout -b + "tên nhánh"                Ví dụ------ git checkout -b feature/quanhv
git branch                                  Ví dụ -- Kiểm tra nhánh hiện tại
